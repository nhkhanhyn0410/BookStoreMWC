<div id="loadingSpinner" class="fixed inset-0 bg-gray-900 bg-opacity-50 flex items-center justify-center z-50 hidden">
    <div class="bg-white rounded-lg p-8 flex flex-col items-center space-y-4 shadow-2xl">
        <!-- Spinner Animation -->
        <div class="relative">
            <div class="animate-spin rounded-full h-16 w-16 border-4 border-gray-200"></div>
            <div
                class="animate-spin rounded-full h-16 w-16 border-4 border-primary-600 border-t-transparent absolute top-0 left-0">
            </div>
        </div>

        <!-- Loading Text -->
        <div class="text-center">
            <p id="loadingText" class="text-gray-700 font-medium">Đang tải...</p>
            <p class="text-sm text-gray-500">Vui lòng chờ trong giây lát</p>
        </div>
    </div>
</div>

<!-- Mini Loading Spinner (for inline use) -->
<div class="inline-loading-spinner hidden">
    <div class="flex items-center justify-center">
        <div class="animate-spin rounded-full h-4 w-4 border-2 border-gray-300 border-t-primary-600 mr-2"></div>
        <span class="text-sm text-gray-600">Đang xử lý...</span>
    </div>
</div>