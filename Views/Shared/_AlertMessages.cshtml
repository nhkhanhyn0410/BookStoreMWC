@if (TempData["SuccessMessage"] != null)
{
    <div class="bg-green-50 border border-green-200 rounded-lg p-4 mb-4 flex items-start shadow-sm">
        <div class="flex-shrink-0">
            <i class="fas fa-check-circle text-green-500 text-xl"></i>
        </div>
        <div class="ml-3 flex-1">
            <h4 class="text-sm font-semibold text-green-800">Thành công!</h4>
            <p class="text-sm text-green-700 mt-1">
                @TempData["SuccessMessage"]
            </p>
        </div>
        <div class="ml-auto pl-3">
            <button onclick="this.parentElement.parentElement.remove()"
                class="text-green-500 hover:text-green-700 transition-colors">
                <i class="fas fa-times"></i>
            </button>
        </div>
    </div>
}

<!-- Error Messages -->
@if (TempData["ErrorMessage"] != null)
{
    <div class="bg-red-50 border border-red-200 rounded-lg p-4 mb-4 flex items-start shadow-sm">
        <div class="flex-shrink-0">
            <i class="fas fa-exclamation-circle text-red-500 text-xl"></i>
        </div>
        <div class="ml-3 flex-1">
            <h4 class="text-sm font-semibold text-red-800">Lỗi!</h4>
            <p class="text-sm text-red-700 mt-1">
                @TempData["ErrorMessage"]
            </p>
        </div>
        <div class="ml-auto pl-3">
            <button onclick="this.parentElement.parentElement.remove()"
                class="text-red-500 hover:text-red-700 transition-colors">
                <i class="fas fa-times"></i>
            </button>
        </div>
    </div>
}

<!-- Warning Messages -->
@if (TempData["WarningMessage"] != null)
{
    <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-4 mb-4 flex items-start shadow-sm">
        <div class="flex-shrink-0">
            <i class="fas fa-exclamation-triangle text-yellow-500 text-xl"></i>
        </div>
        <div class="ml-3 flex-1">
            <h4 class="text-sm font-semibold text-yellow-800">Cảnh báo!</h4>
            <p class="text-sm text-yellow-700 mt-1">
                @TempData["WarningMessage"]
            </p>
        </div>
        <div class="ml-auto pl-3">
            <button onclick="this.parentElement.parentElement.remove()"
                class="text-yellow-500 hover:text-yellow-700 transition-colors">
                <i class="fas fa-times"></i>
            </button>
        </div>
    </div>
}

<!-- Info Messages -->
@if (TempData["InfoMessage"] != null)
{
    <div class="bg-blue-50 border border-blue-200 rounded-lg p-4 mb-4 flex items-start shadow-sm">
        <div class="flex-shrink-0">
            <i class="fas fa-info-circle text-blue-500 text-xl"></i>
        </div>
        <div class="ml-3 flex-1">
            <h4 class="text-sm font-semibold text-blue-800">Thông tin</h4>
            <p class="text-sm text-blue-700 mt-1">
                @TempData["InfoMessage"]
            </p>
        </div>
        <div class="ml-auto pl-3">
            <button onclick="this.parentElement.parentElement.remove()"
                class="text-blue-500 hover:text-blue-700 transition-colors">
                <i class="fas fa-times"></i>
            </button>
        </div>
    </div>
}

<!-- Validation Summary -->
@if (ViewData.ModelState != null && !ViewData.ModelState.IsValid)
{
    <div class="bg-red-50 border border-red-200 rounded-lg p-4 mb-4 shadow-sm">
        <div class="flex items-start">
            <div class="flex-shrink-0">
                <i class="fas fa-exclamation-circle text-red-500 text-xl"></i>
            </div>
            <div class="ml-3">
                <h3 class="text-sm font-semibold text-red-800 mb-3">
                    Vui lòng kiểm tra lại các thông tin sau:
                </h3>
                <ul class="list-disc list-inside text-sm text-red-700 space-y-1">
                    @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                    {
                        <li>@error.ErrorMessage</li>
                    }
                </ul>
            </div>
        </div>
    </div>
}